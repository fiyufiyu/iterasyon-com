# iterasyon.com

Initial brand site for `iterasyon`, positioned as a capital management brand.

## Run locally

Open `index.html` directly in your browser, or run a simple static server:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.

## Next build steps

1. Replace placeholder copy with your exact investment thesis.
2. Connect the contact form to a real endpoint (Resend, Formspree, or custom API).
3. Add legal pages (`Privacy`, `Terms`, `Disclosures`) before going live.
4. Set up analytics and conversion tracking.
